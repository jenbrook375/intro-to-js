// creates number variables
var a = 8;
var b = 26;

// creates variable 'sum'
var sum = a + b;
console.log(sum);

// creates variable 'diff'
var diff = a - b;
console.log(diff);

// creates variable 'mult'
var mult = a * b;
console.log(mult);

// creates variable 'divide'
var divide = b / a;
console.log(divide);

// creates variable 'mod'
var mod = b % a;
console.log(mod);