// creates object fullName that contains kvp's firstName and lastName
var fullName = {
    firstName: 'Jennifer',
    lastName: 'Brookshire',
};
console.log(fullName); // displays content of object fullName
console.log(fullName.firstName + ' ' + fullName.lastName); // displays concatenated kvp's firstName and lastName with space in between